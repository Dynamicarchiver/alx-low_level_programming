#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 * @argc: lenth of command
 * @argv: character in string command
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - 1]);

	return (0);
}
