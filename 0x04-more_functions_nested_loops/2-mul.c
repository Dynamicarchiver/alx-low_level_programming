#include "main.h"
/**
 * mul - Entry point
 * @b: first number to multiply
 * @c: second number to multiply
 * Return: result of the multiplication of a and b
 */

int mul(int a, int b)
{
	int c = a * b;

	_putchar(c);
	return (0);
}
